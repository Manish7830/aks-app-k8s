apiVersion: v1
kind: Service
metadata:
  name: aks-app-frontend
  namespace: aks-app

spec:
  type: ClusterIP

  selector:
    app: aks-app-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80