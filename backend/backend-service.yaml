apiVersion: v1
kind: Service
metadata:
  name: aks-app-backend
  namespace: aks-app

spec:
  type: ClusterIP

  selector:
    app: aks-app-backend

  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP